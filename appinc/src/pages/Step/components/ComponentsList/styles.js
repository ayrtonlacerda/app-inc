import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,

    },
    component: {
        alignItems: 'center',
        padding: 15,
    }
});

export default styles;
